{
  "name": "edgeone-makers-webdav-gateway",
  "version": "2.8.0",
  "private": true,
  "type": "module",
  "description": "EdgeOne Makers WebDAV gateway where every WebDAV request originates from Cloud Function, with Edge KV directory cache, chunked uploads and native CDN direct-link downloads",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
